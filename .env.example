VITE_API_URL=http://localhost:3001

# Cloudflare Turnstile CAPTCHA (optional) — omit to disable CAPTCHA entirely
# Get keys from dash.cloudflare.com (must also set TURNSTILE_SECRET_KEY on the backend)
VITE_TURNSTILE_SITE_KEY=your_turnstile_key
