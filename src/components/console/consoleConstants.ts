/**
 * Shared constants for the admin console. Page size was 20 on some list pages and
 * 25 on others; standardized here so every console list paginates identically.
 */
export const CONSOLE_PAGE_SIZE = 25;
